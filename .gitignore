# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related (user-local state)
*.iml
*.ipr
*.iws
.idea/
# Shared run configs live in `.run/` instead — that path is not ignored,
# so JetBrains IDEs (Android Studio, IntelliJ) pick up project-wide
# run configurations on every clone.

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Env / secrets — never commit real values
.env
.env.local
.env.*.local

# Local-only Claude Code settings (Bash permission grants, etc.)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Supabase CLI working state (cached credentials, link config)
supabase/.temp/

# PowerSync web worker + wasm — re-fetched by build hooks / `dart run
# powersync:setup_web`, not worth committing 1.5 MB of binary
web/sqlite3.wasm
web/powersync_db.worker.js

